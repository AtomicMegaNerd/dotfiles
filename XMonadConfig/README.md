# XMonadConfig

This is the configuration for my XMonad.  I wrapped it in a stack
project because of the fact that Arch's haskell OS does not work with
haskell-language server due to the way it builds its native haskell packages
with dynamic linking.
